<%= form_for(@attachment_file) do |f| %>
  <% if @attachment_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachment_file.errors.count, "error") %> prohibited this attachment_file from being saved:</h2>

      <ul>
      <% @attachment_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
