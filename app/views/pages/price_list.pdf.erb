<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8' />
    <%= wicked_pdf_stylesheet_link_tag "pdf" %>
    <%= wicked_pdf_javascript_include_tag "number_pages" %>
    <style type = 'text/css'>
    div.alwaysbreak { page-break-before: always; }
    div.nobreak:before { clear:both; }
    div.nobreak { page-break-inside: avoid; }
     body {font-family: Tahoma;}  
     table {width: 100%;}
    </style>
  </head>
  <body>
      <body onload='number_pages'>
          <div id="header">
              <%= @title %>
          </div>
          <div id="content">
              
              <% @products.each do |p| %>
                  <% if p != @products.first%><div class = "nobreak"><%end%>
                  <h3>
                      <%= p.s_name %>
                  </h3>
                  <% if p.name != p.s_name%>
                  <h4>
                      <%= p.name %>
                  </h4>
                  <%end%>
                  <div>
                      <div style = "width: 59%; display:inline-block;">
                          <img src = "file:///<%= Rails.root %>/public<%= p.photo.link.thumb.url%>">
                      </div>
                      <div style = "width: 40%; display:inline-block; text-align:center; height:100%;">
                          Цена: <b><%= p.rur_cost.html_safe %></b>
                      </div>
                      
                  </div>
                  <h3>Описание</h3>
                  <%= p.description.html_safe %>
                  <% if !p.table_rows.blank? %>
                      <div>
                          <h3>Технические характеристики </h3>
                          <table>
                              <%= p.table_rows.html_safe %>
                          </table>
                      </div>
                  <% end %>
                  <% if p != @products.first%> </div><%end%>
              <%end%>
              
          </div>
          
      </body>
  </body>
</html>
