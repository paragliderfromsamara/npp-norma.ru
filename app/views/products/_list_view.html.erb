<%v=''%>
<% @products.each do |p| %>
    <%
    
    pHash = {
                      name: p.s_name,
                      link: product_path(p),
                      photo: p.alter_photo_thumb,
                      description: p.note,
                      photo_cols_num: 3
            }
            v += product_row_block(pHash)
    %>
    
<%end%>
<%= v.html_safe %>